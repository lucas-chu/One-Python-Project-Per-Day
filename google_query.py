from google import google


img = Image.open('test.png')
num_page = 3
search_results = google.search("This is my query", num_page)
for result in search_results:
    print(result.description)
# Type 1 is a google search that counts
# Type 2 is a google search with "*" and "Around (3)"
# Type 3 is 3 individual google searches for each question, finding the search with most results
# Wikipedia is a search of the question that 
# Informatics: search of all three answers. 
# img = Image.open('test.png')


n = page.find('"Z0LcW">')
page[7,g]

page.find("</div.>", n) 